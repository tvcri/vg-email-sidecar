# Dependencies
node_modules/
yarn.lock

# package-lock.json is committed: the Dockerfile uses `npm ci`, which requires
# it and pins the dependency tree so every image build is reproducible.

# Environment variables
.env
.env.local
.env.*.local

# OS files
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# IDE
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# Logs
*.log
logs/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build outputs
dist/
build/

vg-mailer-sa-key.json
*-sa-key.json

# Service-request recovery utility (contains member PII)
Recovery_emails.eml
Recovery_2.eml
out.sql
report.json
scratch/
preview/
